cmake_minimum_required (VERSION 2.8.12)
set(CMAKE_CXX_FLAGS "-std=c++11")
project(bloomfilters)
add_definitions(-std=c++11 -DGL_GLEXT_PROTOTYPES -DGLFW_INCLUDE_GLCOREARB)
add_executable(bloomfilters voxels.cc)
add_subdirectory(glfw)
include_directories(glfw/include)
include_directories(gli)
include_directories(glm)
target_link_libraries(bloomfilters glfw ${GLFW_LIBRARIES})
