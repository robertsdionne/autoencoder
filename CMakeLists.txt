cmake_minimum_required (VERSION 2.8.12)
project(autoencoder CXX)
if (NOT WIN32)
  add_definitions(-std=c++11)
endif()
include_directories(libraries/eigen libraries/opencl/include)
add_subdirectory(source)
