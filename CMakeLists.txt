cmake_minimum_required (VERSION 2.8.12)
project(pathtracer CXX)
add_definitions(-DGL_GLEXT_PROTOTYPES)
add_subdirectory(libraries/rsd)
if (NOT WIN32)
  add_definitions(-std=c++11)
endif()
include_directories(libraries/gli)
include_directories(libraries/rsd/libraries/glfw/include)
include_directories(libraries/rsd/libraries/glm)
include_directories(libraries/rsd/libraries/glxw/include)
include_directories(libraries/rsd/source)
add_subdirectory(source)
